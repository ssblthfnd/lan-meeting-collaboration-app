[package]
name = "app-db"
description = "SQLite access: connection management, migrations and repositories. Source of truth."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
app-core = { workspace = true }
thiserror = { workspace = true }
# `audit_logs.metadata` is a JSON column; the read side returns it parsed rather
# than as text the caller must re-parse.
serde_json = { workspace = true }
rusqlite = { workspace = true }
r2d2 = { workspace = true }
r2d2_sqlite = { workspace = true }
refinery = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
