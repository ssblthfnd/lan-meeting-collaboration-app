{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the Host window. Permissions are added only when a feature needs them.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
